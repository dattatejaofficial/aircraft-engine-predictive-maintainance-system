project:
  name: Aircraft Engine Predictive Maintainance System
  version: 0.0.1

artifacts:
  artifact_dir: artifacts

logging:
  logs_dir: logs
  log_level: INFO
  log_format: "[ %(asctime)s ] %(lineno)d %(name)s - %(levelname)s - %(message)s"
  timestamp_format: "%d_%m_%Y_%H_%M_%S"